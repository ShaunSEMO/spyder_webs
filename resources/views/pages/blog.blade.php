<!-- @extends('layouts.index')

@section('blog')
    @foreach($posts as $post)
    <div class="website-type">
      <br>
      <img class="img-fluid" src="{{ $post->image }}" alt="Post header image">
      <h1>{{ $post->title }}</h1>
      <p>{{ $post->body }}</p>
      <br>
    </div>
    @endforeach
@endsection -->


<h1>Hello World. This is a testing post.</h1>
